{
  "permissions": {
    "allow": [
      "mcp__supabase__list_tables",
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(git push *)",
      "Bash(git check-ignore *)",
      "Bash(git config *)"
    ]
  },
  "enabledMcpjsonServers": [
    "supabase"
  ],
  "autoMode": {
    "allow": [
      "$defaults",
      "Non-force `git push` (e.g. `git push origin main`) in this repository (sirfisher-app), done right after implementing a change the user asked for earlier in the same session, does not need a separate per-push confirmation. Force pushes, history rewrites (reset --hard, amend on shared history), and pushes for work the user did not ask for still require explicit confirmation as usual."
    ]
  }
}
